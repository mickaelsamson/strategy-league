// Optional Express integration if your main server does not already serve static folders:
//
// app.use('/moonveil', express.static(path.join(__dirname, 'moonveil')));
//
// If your server already has:
// app.use(express.static(__dirname));
// then you probably do not need to change anything.
